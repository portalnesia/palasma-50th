---
import { EVENT } from "@config/site.ts";
import { LINKS } from "@config/links.ts";
import BackgroundLayers from "@components/atoms/BackgroundLayers.astro";
import FloatingParticles from "@components/atoms/FloatingParticles.astro";
import SideDecor from "@components/atoms/SideDecor.astro";
import SectionTitle from "@components/atoms/SectionTitle.astro";
import OrnamentFlourish from "@components/atoms/OrnamentFlourish.astro";
import CountdownTimer from "@components/molecules/CountdownTimer.astro";
import InfoCard from "@components/molecules/InfoCard.astro";
---

<section id="event-details" class="event-section">
  <BackgroundLayers prefix="event" />

  <FloatingParticles prefix="event" />

  <SideDecor position="left" />
  <SideDecor position="right" />

  <div class="event-container">
    <OrnamentFlourish position="top" />

    <SectionTitle text="Info Acara" />

    <CountdownTimer />

    <InfoCard
      items={[
        {
          icon: "event",
          label: "Acara",
          value: EVENT.name,
          dataReveal: "event-info-row-0",
        },
        {
          icon: "date",
          label: "Tanggal",
          value: EVENT.dateLabel,
          dataReveal: "event-info-row-1",
        },
        {
          icon: "time",
          label: "Waktu",
          value: EVENT.time,
          dataReveal: "event-info-row-2",
        },
        {
          icon: "location",
          label: "Lokasi",
          value: EVENT.location,
          dataReveal: "event-info-row-3",
        },
      ]}
    />

    <div class="event-maps-wrapper" data-reveal="event-maps">
      <div class="event-maps-container">
        <iframe
          class="event-maps-embed"
          src={LINKS.mapsEmbed}
          loading="lazy"
          title="Lokasi Acara — Aranka Tempasan"
          allowfullscreen
          referrerpolicy="no-referrer-when-downgrade"></iframe>
      </div>
    </div>

    <OrnamentFlourish position="bottom" />
  </div>
</section>

<script>
  import { setupEventDetailsReveal, startCountdown } from "@utils/event-details";

  const section = document.getElementById("event-details")!;

  // GSAP scroll reveal
  const revealCleanup = setupEventDetailsReveal(section);

  // Countdown
  const targetDate = new Date("2026-08-29T16:00:00+08:00");
  const elements = {
    days: document.getElementById("countdown-days")!,
    hours: document.getElementById("countdown-hours")!,
    minutes: document.getElementById("countdown-minutes")!,
    seconds: document.getElementById("countdown-seconds")!,
  };
  const countdownCleanup = startCountdown(targetDate, elements);

  document.addEventListener("astro:before-swap", () => {
    revealCleanup();
    countdownCleanup();
  });
</script>

<style>
  /* ===== Event Details Section ===== */
  .event-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 6rem 1.5rem 6rem;
    overflow: hidden;
  }

  .event-container {
    position: relative;
    z-index: 2;
    max-width: 720px;
    width: 100%;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .event-maps-wrapper {
    width: 100%;
    max-width: 580px;
  }

  .event-maps-container {
    position: relative;
    width: 100%;
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid rgba(200, 168, 78, 0.12);
    aspect-ratio: 16 / 9;
  }

  .event-maps-embed {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
  }

  /* ── Responsive (mobile-first) ──────────────────────────── */
  @media (max-width: 768px) {
    .event-section {
      padding: 4rem 1.25rem 5rem;
    }
  }

  @media (max-width: 400px) {
    .event-section {
      padding: 3rem 1rem 4rem;
    }
  }
</style>
