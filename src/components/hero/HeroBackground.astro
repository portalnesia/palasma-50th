---
import { ASSETS } from "@config/assets.ts";

interface Props {
  useBg?: boolean;
}

const { useBg = false } = Astro.props;
---

<div class:list={["hero-bg", { "has-bg-image": useBg }]} aria-hidden="true">
  <div class="hero-bg-gradient-1"></div>
  <div class="hero-bg-gradient-2"></div>
  <div class="hero-bg-gradient-3"></div>
  <div class="hero-bg-gradient-4"></div>
  <div class="hero-bg-grain"></div>
</div>

<style>
  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    background:
      radial-gradient(ellipse 80% 80% at 50% 50%, rgba(200, 168, 78, 0.06) 0%, transparent 70%),
      linear-gradient(
        175deg,
        var(--color-navy-800) 0%,
        var(--color-navy-700) 50%,
        var(--color-navy) 100%
      );
  }

  .hero-bg.has-bg-image {
    background:
      radial-gradient(ellipse 80% 80% at 50% 50%, rgba(200, 168, 78, 0.06) 0%, transparent 70%),
      linear-gradient(
        175deg,
        rgba(8, 73, 140, 0.55) 0%,
        rgba(12, 92, 179, 0.55) 50%,
        rgba(12, 92, 179, 0.55) 100%
      ),
      var(--bg-url) center/cover no-repeat fixed;
  }

  .hero-bg-gradient-1 {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      ellipse 80% 80% at 50% 50%,
      rgba(200, 168, 78, 0.06) 0%,
      transparent 70%
    );
  }

  .hero-bg-gradient-2 {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle 400px at 30% 70%,
      rgba(74, 124, 89, 0.08) 0%,
      transparent 100%
    );
  }

  .hero-bg-gradient-3 {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle 300px at 75% 35%,
      rgba(139, 105, 20, 0.06) 0%,
      transparent 100%
    );
  }

  .hero-bg-gradient-4 {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle 500px at 50% 50%,
      rgba(200, 168, 78, 0.12) 0%,
      rgba(200, 168, 78, 0.04) 40%,
      transparent 70%
    );
    opacity: 0;
  }

  .hero-bg-grain {
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
    background-repeat: repeat;
    background-size: 256px 256px;
    opacity: 0;
    mix-blend-mode: overlay;
  }
</style>
